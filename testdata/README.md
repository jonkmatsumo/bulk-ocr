# Test Data

This directory contains test fixtures for the bulk-ocr pipeline.

## Structure

- `images/` - Sample test images (synthetic, small)
- `empty/` - Empty directory for testing
- `nested/` - Nested structure for recursive testing

## Note

Test images should be small and synthetic to avoid committing large files to the repository. For integration tests, use programmatically generated images or very small fixtures.

